# Continuum proof-first video — timed script

Target: **3:54**. Maximum acceptable master: **3:58**.  
Narration target: 425–450 spoken words.  
After the four-second title, the opening retains the canonical capture in
chronological order until the first disclosed same-run cut.

## 0:00–0:04 — Identify the submission

**Picture:** Title card with `CONTINUUM`, `The agent failed. The promise did
not.`, `Milos Plavsic`, `All Things Agentic Hackathon`, and `August 28, 2026`.
Use only the low Lyria transition cue; no narration.

## 0:04–0:17 — Start with the failure

**Picture:** Native Cloud Run cockpit from frame one. `.run.app` remains
visible. Click `Start fresh recovery` at approximately 0:05.

**Question strap:** `CAN IT CATCH A FAILURE WITH NO ERROR?`

**Voice:**

> This agent owns a two-hundred-and-fifty-thousand-euro supplier obligation.
> The required evidence never arrives. No crash. No exception. I click once;
> from here, the browser only watches.

## 0:17–0:36 — Detect the missing event

**Picture:** Continue the untouched capture through deadline persistence and
`MISSING_EVENT_PUBLISHED`. Point once to the changing phase.

**Voice:**

> Cloud Tasks crosses the persisted deadline. Continuum checks Firestore for
> the evidence that should exist, proves it is absent, and publishes that
> negative-space signal. The first Pub/Sub attempt fails deliberately; the same
> durable workflow resumes on redelivery.

## 0:36–1:04 — Bound Gemini before it reasons

**Picture:** Transparent same-run time cut. Show the three candidate outcomes,
the deterministic eligibility boundary, then ADK/Gemini's cited v19 choice.
Exclude the obsolete handoff-label region from the crop.

**Question strap:** `WHO IS ALLOWED TO TAKE OVER?`

**Voice:**

> Three departments have published immutable successor records. Code rejects
> v20 for health and jurisdiction before the model sees it. Google ADK gives
> Gemini 3.6 only the eligible set and the incident objective. Gemini chooses
> warm successor v19 and cites the trade-off. Deterministic policy validates
> that recommendation before authority moves. The model can reason; it cannot
> mint permission.

## 1:04–1:33 — Transfer purpose, not memory

**Picture:** Same-run minimum-context receipt, predecessor action denial, and
pre-retrieval memory denial. Hold `2 included · 4 excluded` for four seconds.

**Question strap:** `WHAT CROSSES THE HANDOFF?`

**Voice:**

> v17 is fenced at the new authority epoch. Its action is stale, and revoked
> memory is rejected before retrieval. Continuum reconstructs context for the
> purpose: the obligation and two verified facts cross. Prompt injection, a
> predecessor secret, stale inference, and revoked memory stay behind. The
> agent is replaceable. Its promise is not.

## 1:33–2:08 — Finish a useful job

**Picture:** Supplier decision pack. Show GLEIF, VIES availability provenance,
content-addressed receipts, Gemini `ONBOARD`, deterministic admission, and
`SANDBOX_ONLY`.

**Question strap:** `DOES THE SUCCESSOR FINISH REAL WORK?`

**Voice:**

> v19 resumes the actual job. It checks the legal entity through GLEIF and EU
> VAT through VIES. Each normalized observation is hashed before Gemini sees
> it. Gemini produces a cited decision pack; deterministic admission rechecks
> the sources, controls, requested action, and sandbox scope. This is a
> synthetic supplier case, not a commercial decision.

## 2:04 transition / 2:08–2:29 narration — Two deliveries, one effect

**Picture:** At 2:04, disclose the same-run jump with a small
`SAME-RUN TRANSPARENT CUT` badge over the still-visible evidence—never a
full-screen interruption. Two delivery records then converge on the same
idempotency key and one reconciled GitHub work item. Hold the native one-effect
metric.

**Question strap:** `CAN A RETRY DUPLICATE THE EFFECT?`

**Voice:**

> The gateway rechecks identity, epoch, policy, decision pack, context, and
> idempotency in one transaction. Pub/Sub delivers twice. The external queue
> still contains one reversible synthetic work item: one effect, zero
> duplicates under this demonstrated boundary.

## 2:25 transition / 2:29–2:59 narration — Separate execution from truth

**Picture:** Five control-authored artifacts, trust boundary, verifier identity,
direct reads, and verifier-authored artifact six. Hold `VERIFIED`.

**Question strap:** `WHO IS ALLOWED TO DECLARE SUCCESS?`

**Voice:**

> Execution is still only a claim. The control plane exports five
> content-addressed artifacts. A separately deployed read-only verifier resolves
> every digest and rereads authority, supplier state, and provider state. Only
> that identity may issue artifact six: verified, failed, or inconclusive. This
> run is verified.

## 2:59–3:20 — Prove the cloud binding

**Picture:** Exact-release proof strip and native cloud evidence: five private
Cloud Run identities, Firestore, Cloud Tasks, Pub/Sub, Vertex AI, and correlated
Cloud Trace. Show `17 / 17`, `174`, and offline `PASS`.

**Question strap:** `IS THIS ACTUALLY RUNNING ON GOOGLE CLOUD?`

**Voice:**

> The accepted release runs across five private Cloud Run identities with
> Firestore, Cloud Tasks, Pub/Sub, Vertex AI, and Cloud Trace. Its public packet
> contains seventeen required objects and one hundred seventy-four correlated
> spans. The credential-free offline verifier returns pass.

## 3:20–3:44 — Portable boundary, gated learning

**Picture:** Judge architecture until 3:24, then the released model chain for
sixteen seconds. Hold long enough to reach and visibly activate step 4,
`Veo 3.1 + Lyria 3`. Label the latter `RELEASED DERIVATIVE PROOF`.

**Voice:**

> Google Cloud is the reference binding; the Continuity Contract is the
> portable proposal. Only a verified incident may teach: Gemma creates a cited
> drill, and Veo plus Lyria render derivative training media—never authority or
> evidence.

## 3:40–3:54 — Close

**Picture:** End card with project name, motto, author, hackathon, date,
repository, and proof release. Use the transition cue before narration; add no
new product claims.

**Voice:**

> Continuum keeps an institution's promise alive when the agent responsible for
> it fails. The agent failed. The promise did not.
